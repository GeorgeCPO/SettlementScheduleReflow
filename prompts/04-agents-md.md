# 04 — AGENTS.md

## Prompts

> Add an `AGENTS.md` with our conventions: barebones (add things only when needed), no runtime validation,
> `.ts` imports, UTC dates via Luxon, no types ahead of the code, test-first against the e2e suite.

## Outcome / decisions

- `AGENTS.md` covers commands, those conventions, and the domain rules the algorithm must respect.
